{% extends 'admin/base.html.twig' %}

{% block title %}{{ 'app.admin.action.new'|trans({'%gender%': 'female'}) }} {{ 'app.admin.recipes.entity'|trans({'%entity%': 1}) }}{% endblock %}

{% block body %}

<div class="container">
    <h1>{{ 'app.admin.action.new'|trans({'%gender%': 'female'}) }} {{ 'app.admin.recipes.entity'|trans({'%entity%': 1}) }}</h1>
    <a class="btn-primary btn" href="{{ url('admin.recipe.index') }}">All recipes</a>

    <hr>
    {{ form(form) }}
    <hr>

</div>
{% endblock %}
