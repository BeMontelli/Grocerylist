{% extends 'admin/base.html.twig' %}

{% block title %}{{ recipe.title }}{% endblock %}

{% block body %}

<div id="single__recipe">
    <div class="container">

        <div class="cols">
            <div class="col__left">
                <div class="single__image">
                    {% if recipe.thumbnail is not empty %}
                        <img src="{{ recipe.thumbnail }}" alt="{{ recipe.title }}">
                    {% else %}
                        <span>NO IMAGE</span>
                    {% endif %}
                </div>

                <div class="single__price"><strong>Price</strong>: <span>{{ recipe.price }} €</span></div>

                <div class="single__categories">
                    {% if recipe.category is defined and recipe.category.title is defined and recipe.category.title is not empty %}
                        <span><a href="{{ url('admin.category.show', {id: recipe.category.id,slug: recipe.category.slug}) }}">{{ recipe.category.title }}</a></span>
                    {% else %}
                        <span>---</span>
                    {% endif %}
                </div>
            </div>
            <div class="col__right">
                <div class="single__pres">
                    <div class="single__title">
                        <h1>{{ recipe.title }}</h1>
                        <a class="btn-primary btn mb-2" href="{{ url('admin.recipe.index') }}">All recipes</a>
                    </div>
                    <div class="single__content">
                        <div class="collapse show" id="collapseExample">
                            <div class="card card-body">
                                {{ recipe.content | raw | nl2br }}
                            </div>
                        </div>
                    </div>
                </div>
                <button class="btn btn-primary mb-3" type="button" data-bs-toggle="collapse" data-bs-target="#collapseExample" aria-expanded="false" aria-controls="collapseExample">
                    {{ 'app.admin.action.edit'|trans({'%gender%': 'female'}) }}
                </button>
                {% include 'admin/partials/flashes.html.twig' %}
                <div class="single__edit mb-3">
                    <div class="collapse" id="collapseExample">
                        <div class="card card-body">
                            {{ include('admin/recipe/_form.html.twig') }}
                        </div>
                    </div>
                </div>

                <div class="single__form">
                    <div class="collapse show" id="collapseExample">
                        <div class="card card-body">
                            {{ include('admin/recipe/_form_to_list.html.twig') }}
                        </div>
                    </div>
                </div>
            </div>
        </div>

    </div>
</div>
{% endblock %}
