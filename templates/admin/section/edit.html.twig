{% extends 'admin/base.html.twig' %}

{% block title %}Edit - {{ section.title }}{% endblock %}

{% block body %}

    {{ include('admin/partials/page_title.html.twig', {
        'icon': "<i class='icon__page_title bx bxs-category' ></i>",
        'title': 'Edit -' ~ section.title,
        'links': [
            {
                'url': path('admin.section.index'),
                'label': "<i class='bx bx-left-arrow-alt'></i> All sections",
                'class': 'btn_page_title_overflow btn-primary btn'
            }
        ]
    }) }}

    <div class="container">

        {{ include('admin/section/_form.html.twig', {'button_label': "<i class='bx bx-edit-alt' ></i>"|raw}) }}

        {{ include('admin/section/_delete_form.html.twig') }}
    </div>
{% endblock %}
