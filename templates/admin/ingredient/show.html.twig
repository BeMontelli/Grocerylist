{% extends 'admin/base.html.twig' %}

{% block title %}{{ ingredient.title }}{% endblock %}

{% block body %}

<div class="container">
    <h1>{{ ingredient.title }}</h1>
    <div class="actions">
        <a class="btn-primary btn" href="{{ url('admin.ingredient.edit', { id: ingredient.id}) }}">{{ 'app.admin.action.edit'|trans }}</a>
    <a class="btn-primary btn" href="{{ url('admin.ingredient.index') }}">All ingredients</a>
    </div>
</div>
{% endblock %}
