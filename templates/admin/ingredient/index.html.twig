{% extends 'admin/base.html.twig' %}

{% block title %}Ingredients{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">
    <h1>Ingredients</h1>
    <a class="btn-primary btn" href="{{ url('admin.ingredient.create') }}">Create ingredient</a>

    <hr>
    {% include 'admin/partials/flashes.html.twig' %}

    {# total items count #}
    <div class="count">
        Results count : {{ ingredients.getTotalItemCount }}
    </div>
    <table>
        <tr>
            {# sorting of properties based on query components #}
            <th>{{ knp_pagination_sortable(ingredients, 'Id', 'r.id') }}</th>
            <th{% if ingredients.isSorted('a.title') %} class="sorted"{% endif %}>
                {{ knp_pagination_sortable(ingredients, 'Title', 'r.title') }}
            </th>
            <th{% if ingredients.isSorted('a.slug') %} class="sorted"{% endif %}>
                {{ knp_pagination_sortable(ingredients, 'Slug', 'r.slug') }}
            </th>
            <th class="actions">Actions</th>
        </tr>

        {# table body #}
        {% for ingredient in ingredients %}
            <tr {% if loop.index is odd %}class="color"{% endif %}>
                <td>{{ ingredient.id }}</td>
                <td><a href="{{ url('admin.ingredient.show', {slug: ingredient.slug, id: ingredient.id}) }}">{{ ingredient.title }}</a></td>
                <td>{{ ingredient.slug }}</td>
                <td class="actions">
                    <a class="btn-primary btn" href="{{ url('admin.ingredient.edit', { id: ingredient.id}) }}">Editer</a>
                    <form action="{{ url('admin.ingredient.delete', { id: ingredient.id}) }}" method="post">
                        <input type="hidden" name="_method" value="DELETE">
                        <button class="btn-primary btn" type="submit">Supprimer</button>
                    </form>
                </td>
            </tr>
        {% endfor %}
    </table>
    {# display navigation #}
    <div class="navigation">
        {{ knp_pagination_render(ingredients) }}
    </div>
</div>
{% endblock %}
