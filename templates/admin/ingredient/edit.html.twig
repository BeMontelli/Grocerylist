{% extends 'admin/base.html.twig' %}

{% block title %}{{ 'app.admin.action.edit'|trans }} - {{ ingredient.title }}{% endblock %}

{% block body %}

    {{ include('admin/partials/page_title.html.twig', {
        'icon': "<i class='icon__page_title bx bxs-bowl-rice' ></i>",
        'title': 'app.admin.action.edit'|trans ~ ' - ' ~ ingredient.title,
        'links': [
            {
                'url': path('admin.ingredient.index'),
                'label': "<i class='bx bx-left-arrow-alt'></i>" ~ 'app.admin.ingredients.title'|trans,
                'class': 'btn_page_title_overflow btn-primary btn'
            }
        ]
    }) }}

    <div class="container">
        <div class="list__form mb-4">
            {{ include('admin/ingredient/_form.html.twig', {'button_label': "<i class='bx bx-edit-alt' ></i>"|raw}) }}
        </div>

    </div>
{% endblock %}
