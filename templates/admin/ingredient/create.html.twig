{% extends 'admin/base.html.twig' %}

{% block title %}{{ 'app.admin.action.new'|trans({'%gender%': 'male'}) }} {{ 'app.admin.ingredients.entity'|trans({'%entity%': 1}) }}{% endblock %}

{% block body %}

<div class="container">
    <h1>{{ 'app.admin.action.new'|trans({'%gender%': 'male'}) }} {{ 'app.admin.ingredients.entity'|trans({'%entity%': 1}) }}</h1>
    <a class="btn-primary btn" href="{{ url('admin.ingredient.index') }}">All ingredients</a>

    <hr>
    {{ form(form) }}
    <hr>

</div>
{% endblock %}
