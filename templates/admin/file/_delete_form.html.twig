<form 
    method="post" 
    action="{{ path('admin.file.delete', {'id': file.id}) }}" 
    class="delete__form" 
    onsubmit="return confirm('Are you sure you want to delete this item?');"
    data-title="Delete item ?"
    data-txts="<p>Are you sure you want to delete this item ?</p>"
    data-confirm="<i class='bx bx-trash' ></i>"
    data-cancel="<i class='bx bx-arrow-back' ></i>"
>
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ file.id) }}">
    <button class="btn__confirm btn-remove btn" type="submit"><i class='bx bx-trash'></i></button>
</form>
