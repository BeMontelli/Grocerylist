{% extends 'admin/base.html.twig' %}

{% block title %}{{ product.title }}{% endblock %}

{% block body %}

<div class="container">
    <h1>{{ product.title }}</h1>
    <a class="btn-primary btn" href="{{ url('admin.product.index') }}">All products</a>
</div>
{% endblock %}
