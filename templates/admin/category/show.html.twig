{% extends 'admin/base.html.twig' %}

{% block title %}{{ category.title }}{% endblock %}

{% block body %}

<div class="container">
    <h1>{{ category.title }}</h1>
    <a class="btn-primary btn" href="{{ url('admin.category.index') }}">All categories</a>
</div>
{% endblock %}
