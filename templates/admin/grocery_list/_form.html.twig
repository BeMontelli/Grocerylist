{{ form_start(form) }}
    {{ form_row(form.title) }}
    {{ form_row(form.slug) }}
    {{ form_row(form.save, {'label':  'app.admin.action.update'|trans}) }}
    {{ form_widget(form) }}
{{ form_end(form) }}
