{% extends 'admin/base.html.twig' %}

{% block title %}New GroceryList{% endblock %}

{% block body %}
    <div class="container">
        <h1>Create new GroceryList</h1>

        {{ include('admin/grocery_list/_form.html.twig') }}

        <a class="btn-primary btn" href="{{ path('admin.list.index') }}">back to list</a>
    </div>
{% endblock %}
