<form method="post" action="{{ path('admin.group.delete', {'id': group.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ group.id) }}">
    <button class="btn-primary btn">{{ 'app.admin.action.delete'|trans }}</button>
</form>
